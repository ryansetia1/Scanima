# Kredensial. Tidak pernah di-commit.
.env
.env.*
!.env.example
*.token

node_modules/

# Hasil eval: mahal untuk dibuat, tapi biner dan besar.
# Simpan lokal, arsipkan manual kalau perlu dibandingkan antar versi prompt.
eval/results/
eval/photos/*
!eval/photos/.gitkeep
!eval/photos/README.md
build/

# Audit visual roster Seeker: turunan gratis dari art yang sudah ter-commit,
# jadi `--strip` membuatnya ulang kapan saja tanpa panggilan API.
backend/generated/seekers/roster_strip.png

# Godot
game/.godot/
game/.godotiq/
game/android/
game/export_presets.cfg
*.translation

# macOS
.DS_Store
supabase/backup-*
